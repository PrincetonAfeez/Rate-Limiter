# Runtime dependencies for capstone-rate-limiter.
#
# The library intentionally has no required third-party packages at runtime.
# Install the package itself in editable mode:
#
#   pip install -r requirements.txt
#
-e .
