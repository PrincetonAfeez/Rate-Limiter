# Optional YAML config support (PyYAML).
#
# Without PyYAML, load_config() uses a minimal built-in YAML parser for the
# documented config subset. With PyYAML installed, full YAML files are supported.
#
# Equivalent to: pip install -e ".[yaml]"
#
#   pip install -r requirements-yaml.txt
#
-r requirements.txt

PyYAML>=6
